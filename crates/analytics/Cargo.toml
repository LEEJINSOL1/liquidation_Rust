[package]
name = "analytics"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
tracing = "0.1"
serde = { version = "1", features = ["derive"] }
statrs = "0.16"
itertools = "0.12"
serde_json = "1"
storage = { path = "../storage" }
